# Sample release keystore configuration. Copy this file to "android/keystore.properties"
# and replace the placeholder values with your actual keystore credentials. Do not
# commit the populated file to source control.
storeFile=release.keystore
storePassword=CHANGEME_STORE_PASSWORD
keyAlias=release
keyPassword=CHANGEME_KEY_PASSWORD
